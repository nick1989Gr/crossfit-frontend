import { makeStyles } from "@material-ui/core/styles";

export const useProfileStyles = makeStyles(() => ({
  root: {
    flexGrow: 1,
  },
  card: {
    textAlign: "center",
    top: 20,
    left: "40%",
    marginLeft: 20,
  },
}));
