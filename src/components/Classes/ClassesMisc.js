import { makeStyles } from "@material-ui/core/styles";

export const useClassesStyles = makeStyles(() => ({
  root: {
    flexGrow: 1,
  },
  card: {
    textAlign: "center",
    top: 20,
    left: "30%",
    marginLeft: 20,
  },
}));
